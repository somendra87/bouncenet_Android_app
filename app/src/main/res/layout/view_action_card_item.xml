<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/actionCardItemTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Action Card Item Title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:textSize="16sp"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        />

    <TextView
        android:id="@+id/actionCardItemSubTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="Action Card SubTitle"
        android:textStyle="bold"
        android:textColor="@color/text_secondary"
        app:layout_constraintTop_toBottomOf="@id/actionCardItemTitle"
        app:layout_constraintStart_toStartOf="parent"
        />

    <TextView
        android:id="@+id/actionCardItemBtn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:text="Cta Button →"
        android:textStyle="bold"
        android:textColor="@color/text_secondary"
        app:layout_constraintTop_toBottomOf="@id/actionCardItemSubTitle"
        app:layout_constraintStart_toStartOf="parent"
        />


</androidx.constraintlayout.widget.ConstraintLayout>